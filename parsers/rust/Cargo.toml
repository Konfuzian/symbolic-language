[package]
name = "sym-parser"
version = "0.1.0"
edition = "2021"
description = "Parser for the SYM data format"
license = "MIT"

[dependencies]
thiserror = "1.0"
serde_json = "1.0"
serde_yaml = "0.9"
toml = "0.8"

[dev-dependencies]
